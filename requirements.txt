# --- Microsoft Foundry Agent Service SDK (new Foundry projects API) ---------
# azure-ai-projects 2.x exposes the new agents API used here:
#   * project.agents.create_version(...) + PromptAgentDefinition
#   * project.get_openai_client() -> OpenAI-compatible Responses API
#   * MCPTool with inline auth `headers` and tool-call approval
# Docs: https://learn.microsoft.com/azure/foundry/agents/quickstarts/prompt-agent
azure-ai-projects>=2.3.0,<3
azure-identity>=1.24,<2
# The Responses API surface (conversations/responses + MCP approval types)
# builds on the OpenAI client. azure-ai-projects 2.x pulls this in, but we pin a
# floor because we import `openai.types.responses.response_input_param`.
openai>=1.109

# --- Google OAuth 2.0 -------------------------------------------------------
# Used to obtain and refresh the Google Drive access token locally.
google-auth>=2.35,<3
google-auth-oauthlib>=1.2,<2

# --- Utilities --------------------------------------------------------------
python-dotenv>=1.0,<2
requests>=2.32,<3
