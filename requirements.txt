# --- Azure Foundry Agent Service SDK ---------------------------------------
# Pre-release pins are REQUIRED: the MCP tool with custom auth headers and
# tool-call approval lives in azure-ai-agents >= 1.2.0b3, and azure-ai-projects
# 1.1.0b4 is the matching project client that still exposes `.agents`.
# (azure-ai-projects 2.x replaced `.agents` with an OpenAI-compatible surface.)
# Exact pins let pip install these pre-releases without a global --pre flag,
# so transitive dependencies still resolve to stable versions.
azure-ai-projects==1.1.0b4
azure-ai-agents==1.2.0b6
azure-identity>=1.24,<2

# --- Google OAuth 2.0 -------------------------------------------------------
# Used to obtain and refresh the Google Drive access token locally.
google-auth>=2.35,<3
google-auth-oauthlib>=1.2,<2

# --- Utilities --------------------------------------------------------------
python-dotenv>=1.0,<2
requests>=2.32,<3
